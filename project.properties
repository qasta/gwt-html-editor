# Modify these to fit your configuration

# options: linux, windows, mac
gwt.os = windows

gwt.installation.dir = C:/Java/gwt-windows-1.4.60


# do not touch these unless you have to!
editor.version = 0.1.4-preview

build.dir = ../build
classes.dir = ${build.dir}/classes
jar.dir = ${build.dir}/jar
gwt.output = ${build.dir}/web

jar.file = ${build.dir}/editor-${editor.version}.jar

source.dir = src
test.dir = test

gwt.jar.user = ${gwt.installation.dir}/gwt-user.jar
gwt.jar.servlet = ${gwt.installation.dir}/gwt-servlet.jar
gwt.jar.dev = ${gwt.installation.dir}/gwt-dev-${gwt.os}.jar
